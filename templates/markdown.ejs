<% commits.forEach(function (commit) { %>
* __<%= commit.title %>__

    [<%= commit.authorName %>](<%= commit.authorEmail %>) - <%= commit.committerDate %>
    
    <%= commit.messageLines.join("\n    ") %>
<% }) %>